spring:
  application:
    name: mealmates
  datasource:
    url: jdbc:postgresql://100.86.62.47:5433/studydb2
    username: studyuser2
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 100